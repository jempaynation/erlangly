/**
 * Erlangly Environment Configuration Template
 * 
 * Instructions:
 * 1. Copy this file to `js/config.js`
 * 2. Replace the placeholder values with your Supabase Project URL and Public Anon Key
 * 3. `js/config.js` is git-ignored and will NEVER be committed to version control
 */

window.ERLANGLY_CONFIG = {
  SUPABASE_URL: 'https://your-project-ref.supabase.co',
  SUPABASE_ANON_KEY: 'your-public-anon-key'
};
